{
  // Archivo "solución": no compila nada por sí mismo, orquesta el build
  // incremental de los paquetes en orden topológico (`tsc -b`).
  // Para chequear tipos de TODO el repo (incluidos los tests) se usa
  // tsconfig.typecheck.json, que resuelve @mqs/* contra el código fuente.
  "files": [],
  "references": [
    { "path": "packages/qr-core" },
    { "path": "packages/baneco-gateway" },
    { "path": "packages/yape-scraper" },
    { "path": "packages/wa-bridge" },
    { "path": "packages/functions" },
    { "path": "packages/demo-web" },
    { "path": "tools/baneco-b0" }
  ]
}
